const initialState = [
	"Инстаграм",
	"От друзей",
	"Реклама",
	"Поисковые системы",
	"Другое"
];

export function sources(state=initialState, action){
	switch (action.type) {
		default: return state;
	}
}